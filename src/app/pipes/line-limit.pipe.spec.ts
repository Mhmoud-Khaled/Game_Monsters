import { LineLimitPipe } from './line-limit.pipe';

describe('LineLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new LineLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
